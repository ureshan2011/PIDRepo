import { PageHeader } from "@/components/shell/PageHeader";

// PHASE 1 PLACEHOLDER — replace with the Executive Overview (briefing, agenda,
// deadlines). Do not change the route or the shell. BUILD_SPEC §8.
export default function OverviewPage() {
  return (
    <div>
      <PageHeader title="Overview" description="Your daily briefing and agenda." />
      <p className="text-sm text-muted-foreground">Coming soon.</p>
    </div>
  );
}
