import { PageHeader } from "@/components/shell/PageHeader";

// PHASE 1 PLACEHOLDER — replace with Goals + milestones UI. BUILD_SPEC §8.
export default function GoalsPage() {
  return (
    <div>
      <PageHeader title="Goals" description="Define outcomes and milestones." />
      <p className="text-sm text-muted-foreground">Coming soon.</p>
    </div>
  );
}
