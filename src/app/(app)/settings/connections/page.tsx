import { PageHeader } from "@/components/shell/PageHeader";

// PHASE 1 PLACEHOLDER — replace with the ConnectionHealthList (sources + sync
// health). Do not change the route. BUILD_SPEC §8 / §9.
export default function ConnectionsPage() {
  return (
    <div>
      <PageHeader title="Connections" description="Connector accounts and sync health." />
      <p className="text-sm text-muted-foreground">Coming soon.</p>
    </div>
  );
}
